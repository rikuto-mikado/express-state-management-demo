# Force Linguist to detect .js files as JavaScript
*.js linguist-language=JavaScript

# Force Linguist to detect .ejs template files as HTML or EJS
*.ejs linguist-language=EJS

# Mark package-lock.json as vendored so it doesn't skew stats
package-lock.json linguist-vendored=true
